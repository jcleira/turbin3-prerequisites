# airdrop project specific .gitignore file
./airdrop/.yarn/*
!./airdrop/yarn/patches
!./airdrop/yarn/plugins
!./airdrop/yarn/releases
!./airdrop/yarn/sdks
!./airdrop/yarn/versions

# Swap the comments on the following lines if you wish to use zero-installs
# In that case, don't forget to run `yarn config set enableGlobalCache false`!
# Documentation here: https://yarnpkg.com/features/caching#zero-installs

#!.yarn/cache
./airdrop/.pnp.*

# Prevent any wallet files from being committed.
./airdrop/*wallet*.json

